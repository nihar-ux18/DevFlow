export const api = {
  // TODO: Replace with axios instance once installed
  get: async (url: string) => { console.log(url); return {}; },
  post: async (url: string, data: any) => { console.log(url, data); return {}; }
};
